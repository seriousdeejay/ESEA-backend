# from django.db import models

# class NetworkTeam(models.Model):
#     network = models.OneToOneField('Network', related_name='team', on_delete=models.CASCADE)
    
#     def __str__(self):
#         return f"{self.network.name}'s Team"